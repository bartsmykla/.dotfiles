function git-push-origin-first-no-verify-force \
  --description "Push to origin if it exists, otherwise fallback to upstream with --no-verify and --force flags"

  af shortcuts abbreviations gp --remote origin-first --no-verify --force
end

