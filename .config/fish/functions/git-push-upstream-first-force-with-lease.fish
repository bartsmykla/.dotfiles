function git-push-upstream-first-force-with-lease \
  --description "Push to upstream if it exists, otherwise fallback to origin with --force-with-lease flag"

  af shortcuts abbreviations gp --remote upstream-first --force-with-lease
end

