function git-push-upstream-first-no-verify-force \
  --description "Push to upstream if it exists, otherwise fallback to origin with --no-verify and --force flags"

  af shortcuts abbreviations gp --remote upstream-first --no-verify --force
end

