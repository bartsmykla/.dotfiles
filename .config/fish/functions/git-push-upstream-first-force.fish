function git-push-upstream-first-force \
  --description "Push to upstream if it exists, otherwise fallback to origin with --force flag"

  af shortcuts abbreviations gp --remote upstream-first --force
end

