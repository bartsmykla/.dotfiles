function git-push-origin-first-force \
  --description "Push to origin if it exists, otherwise fallback to upstream with --force flag"

  af shortcuts abbreviations gp --remote origin-first --force
end

